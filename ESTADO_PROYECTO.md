# وضعیت بخش‌های پروژه

| # | بخش | پوشه | وضعیت |
|---|---|---|---|
| 1 | مدل SAP2000 ماژول ۴۰ متری Muelle de Trasmallo و مقایسه با Anejo 10 | `sap2000/`، `documentacion/` | تمام‌شده |
| 2 | مدل‌سازی قدم‌به‌قدم در SAP (یادگیری دستی، بدون import) | — | **نیمه‌تمام.** قدم ۱ (Grid) و قدم ۲ (مصالح HA-50 / HA-35) انجام شده؛ قدم ۳ (مقطع شمع 40×40 با ضرایب اصلاح) توضیح داده شده ولی تأیید نشده. ادامه از قدم ۳ به بعد تا Run. |
| 3 | طراحی بتن‌آرمه طبق Código Estructural و طراحی بتن در SAP | `diseno/` | **نیمه‌تمام.** روش پایتون تمام شده (`diseno/python/`). طبق تصمیم Eva (سرپرست): مدل SAP همان Muelle de Trasmallo ادامه پیدا کند تا نیروهای طراحی از آن گرفته شود، و **زلزله طبق پروژهٔ Rover به‌روز شود** (مرجع‌ها در `diseno/referencia_rover/`: Anejo Nacional UNE-EN 1998-1، ag,R = 0.15g، K = 1، خاک D، ρ = 0.85، S = 1.91، ac = 0.2435g). زلزله اضافه شد (فایل‌های `*_sismo`، بدون تغییر در فایل‌های قبلی). کنترل مستقل پایتون: با طیف الاستیک Rover (μ = 1) زلزله حاکم است و شمع‌ها (η ≈ 1.6 تا 1.85) و خمش منفی تیرها (η ≈ 1.3) رد می‌شوند؛ با μ = 2 قبول. تصمیم دربارهٔ ضریب شکل‌پذیری یا تغییر مقطع با سرپرست. بعد: سپس نتایج SAP از کاربر و بعد از آن گزارش فارسی (حداکثر یک صفحه خلاصهٔ کار قبلی در ابتدا) و گزارش فنی انگلیسی (Word) با شکل‌های SAP در برابر Anejo 10. |

## نکات ثابت برای همهٔ گزارش‌ها

- جملهٔ «این متن برای کسی نوشته شده که …» یا هر مقدمهٔ مشابه دربارهٔ مخاطب در هیچ گزارشی نوشته نشود.
- گزارش‌ها قبل از رسیدن نتایج لازم (مثلاً نتایج SAP) نوشته نشوند.
- روش‌های غیر از SAP با اسکریپت‌هایشان در یک پوشهٔ جدا (`diseno/python/`) نگه داشته و کامل توضیح داده شوند.
