# وضعیت بخش‌های پروژه

| # | بخش | پوشه | وضعیت |
|---|---|---|---|
| 1 | مدل SAP2000 ماژول ۴۰ متری Muelle de Trasmallo و مقایسه با Anejo 10 | `sap2000/`، `documentacion/` | تمام‌شده |
| 2 | مدل‌سازی قدم‌به‌قدم در SAP (یادگیری دستی، بدون import) | — | **نیمه‌تمام.** قدم ۱ (Grid) و قدم ۲ (مصالح HA-50 / HA-35) انجام شده؛ قدم ۳ (مقطع شمع 40×40 با ضرایب اصلاح) توضیح داده شده ولی تأیید نشده. ادامه از قدم ۳ به بعد تا Run. |
| 3 | طراحی بتن‌آرمه طبق Código Estructural و طراحی بتن در SAP | `diseno/` | **نیمه‌تمام، منتظر نتایج طراحی SAP از کاربر.** روش پایتون تمام شده (`diseno/python/`). بعد از دریافت نتایج SAP: گزارش فارسی (با حداکثر یک صفحه خلاصهٔ کار قبلی شروع شود)، گزارش فنی انگلیسی (Word، متمرکز بر نتایج SAP و مقایسه با Anejo 10)، شکل‌های SAP در برابر Anejo 10. |
| 4 | پروژهٔ Rover (اسکلهٔ Jet Ski، رودخانهٔ Júcar) | `proyecto_rover/` | شروع‌شده: فایل‌ها ذخیره و شناسایی شدند. PDF مطالعهٔ ژئوتکنیک هنوز در repo نیست (فقط متن آن). |

## نکات ثابت برای همهٔ گزارش‌ها

- جملهٔ «این متن برای کسی نوشته شده که …» یا هر مقدمهٔ مشابه دربارهٔ مخاطب در هیچ گزارشی نوشته نشود.
- گزارش‌ها قبل از رسیدن نتایج لازم (مثلاً نتایج SAP) نوشته نشوند.
- روش‌های غیر از SAP با اسکریپت‌هایشان در یک پوشهٔ جدا (`diseno/python/`) نگه داشته و کامل توضیح داده شوند.
